<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Backend:etc/menu.xsd">
    <menu>
        <add id="${Menu_ID}" title="${Menu_Title}" translate="title" module="${VendorName_ModuleName}" sortOrder="${Sort_Order}"
             parent="${Parent_Menu_ID}" action="${Route_Path}" resource="${ACL_Resource}" />
    </menu>
</config>